name: happy_pleace
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter
  table_calendar: ^3.1.2
  

dev_dependencies:
  flutter_launcher_icons: "^0.9.2"  
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
flutter_icons:
  android: true  
  ios: true  
  image_path: 'assets/images/appicon/'

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/images/appicon/happy_place_icon.png
    - assets/locations/
    - assets/profilimage/
