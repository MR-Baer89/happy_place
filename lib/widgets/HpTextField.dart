import 'package:flutter/material.dart';

class HpTextField extends StatelessWidget {
  const HpTextField({super.key});

  @override
  Widget build(BuildContext context) {
    return const TextField();
  }
}
